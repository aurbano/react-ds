export type Point = {
  x: number;
  y: number;
};

export type Projection = {
  x: [number, number];
  y: [number, number];
};

export type Box = {
  left: number;
  top: number;
  width: number;
  height: number;
};
